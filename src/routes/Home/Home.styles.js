import {
    StyleSheet
} from "react-native";

export const styles = StyleSheet.create({
    logoutButton: {
        position: 'absolute',
        top: 10,
        right: 10,
        opacity: 1
    }
})