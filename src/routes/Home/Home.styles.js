import {
    StyleSheet
} from "react-native";

export const styles = StyleSheet.create({
    logoutButtonWrapper:{
        padding: 20,
        position: 'absolute',
        top: 10,
        right: 10,
        backgroundColor: 'transparent',
    },
    logoutButton: {
    }
})